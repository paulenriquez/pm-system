<%= simple_form_for @pump_nozzle, html: {class: 'form-partial'} do |pump_nozzle| %> 
    <div class="alert alert-warning mt-2 p-2 pl-3 pr-3 fs-pt9">
        You won't be able to modify this pump nozzle once it has been created
        due to the number of entities that are dependent on this record.
    </div>
    
    <div class="form-row">
        <div class="form-group col mb-0">
            <label class="required">Pump Island</label>
            <input class="form-control" v-model="pumpNozzle.pumpIslandNum">
        </div>
        <div class="form-group col mb-0">
            <label class="required">Loading Pt.</label>
            <input class="form-control" v-model="pumpNozzle.loadingPointNum">
        </div>
        <div class="form-group col mb-0">
            <label class="required">Nozzle</label>
            <input class="form-control" v-model="pumpNozzle.nozzleNum">
        </div>
    </div>
    <div class="form-row mt-2">
        <div class="form-group col">
            <p class="fs-pt8 text-muted mb-2">
                Ensure that the pump island, loading point, and nozzle are correct since
                these will be used by other records as reference to this pump nozzle.
            </p>
        </div>
    </div>

    <div class="form-row">
        <div class="form-group col-6">
            <label class="required">Fuel</label>
            <%= 
                render 'shared/product_selector',
                show_only: 'Fuel',
                default_text: 'Select fuel...',
                html: { 
                    id: 'select-product',
                    class: 'full-width'
                }
            %>
        </div>
    </div>


    <%= pump_nozzle.submit 'Save', class: 'btn btn-primary' %>

    <!-- HIDDEN FIELDS -->
    <%= pump_nozzle.hidden_field :pump_island_num,   id: 'pump_nozzle_pump_island_num',   'v-bind:value': 'pumpNozzle.pumpIslandNum'   %>
    <%= pump_nozzle.hidden_field :loading_point_num, id: 'pump_nozzle_loading_point_num', 'v-bind:value': 'pumpNozzle.loadingPointNum' %>
    <%= pump_nozzle.hidden_field :nozzle_num,        id: 'pump_nozzle_nozzle_num',        'v-bind:value': 'pumpNozzle.nozzleNum'       %>
    <%= pump_nozzle.hidden_field :product_id ,       id: 'pump_nozzle_product_id',        'v-bind:value': 'pumpNozzle.productId'       %>
<% end %>